import 'package:flutter/material.dart';

const Color primaryColor = Color.fromRGBO(1, 94, 203, 1);
const Color primaryColorDark = Color.fromRGBO(47, 87, 159, 1);
const Color colorAccent = Color.fromRGBO(250, 178, 6, 1);
const Color buttonColor = Colors.greenAccent;

ButtonStyle mainBtnStyle =  ElevatedButton.styleFrom(
    primary: buttonColor,
    elevation: 0,
);